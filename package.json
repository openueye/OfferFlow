{
  "name": "offerflow",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "test": "node --test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "prepare": "husky",
    "postinstall": "prisma generate --schema prisma/schema.pg.prisma",
    "db:generate": "npm run db:generate:sqlite",
    "db:generate:sqlite": "prisma generate --schema prisma/schema.sqlite.prisma",
    "db:generate:pg": "prisma generate --schema prisma/schema.pg.prisma",
    "db:push": "npm run db:push:sqlite",
    "db:push:sqlite": "prisma db push --schema prisma/schema.sqlite.prisma",
    "db:push:pg": "prisma db push --schema prisma/schema.pg.prisma",
    "db:studio": "prisma studio --schema prisma/schema.sqlite.prisma",
    "db:sqlite": "node scripts/switch-db.mjs sqlite && npm run db:generate:sqlite && echo \"Switched to SQLite (local dev)\"",
    "db:pg": "node scripts/switch-db.mjs pg && npm run db:generate:pg && echo \"Switched to PostgreSQL (Vercel deploy)\""
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "@tailwindcss/postcss": "^4.3.0",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.3",
    "mammoth": "^1.12.0",
    "next": "^16.2.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "recharts": "^3.8.1",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2",
    "prisma": "^6.5.0"
  }
}
